# Pull request

Interactive workflow for preparing and opening GitHub pull requests from Pi.

Use `/pr` or `/pull-request` to review the branch, handle uncommitted changes, draft the title and description, push the branch, and create the pull request.

Requires an authenticated GitHub CLI (`gh`) and an active Git branch.
